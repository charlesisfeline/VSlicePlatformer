# vs code shits
.vs/*
!.code-workspace
.vscode/.temp
!*.hxml

# ! means a file should be added regardless of it it gets ignored prior.
# Including to allow others to use already set-up configuration tweaked for the project.
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# macOS being a poosy!!!
.DS_Store
__MACOSX/
.AppleDouble
.LSOverride

# js stuff
node_modules/
package.json
package-lock.json

# Local history which shouldn't be shared.
.history
.ionide

# Junk files that might appear on USB shits
.Spotlight-V100
.Trashes

# misc
Thumbs.db
desktop.ini
RECOVER_*.fla
* - Copy*
._*
dump/
